PORT=4000
# Local: postgresql://user:pass@localhost:5432/poolmarket
# Supabase: postgresql://postgres:Nischal$0958@db.xxxxx.supabase.co:5432/postgres
DATABASE_URL=postgresql://user:pass@localhost:5432/poolmarket
# Redis: optional. Used for leaderboard/CRS cache and rate limits. If wrong or missing, app still runs (cache/rate-limit disabled).
# Local (no auth): redis://localhost:6379
# Upstash (use your password): rediss://default:YOUR_PASSWORD@xxx.upstash.io:6379
REDIS_URL=redis://localhost:6379
# Local dev; production: https://pompomm.in (and https://www.pompomm.in if you use www)
FRONTEND_ORIGIN=http://localhost:3000
# FRONTEND_ORIGIN=https://pompomm.in,https://www.pompomm.in
UPLOAD_DIR=./uploads
R2_PUBLIC_URL=
NODE_ENV=development
# Raw log retention: 30 = small brands (default), 90 = enterprise. Used by distribution:archive and prune:logs.
# LOG_RETENTION_DAYS=30
# JWT_SECRET=your-secret-for-production
# Creator tier auto-update from CRS. Set to false to disable (default: true).
# ENABLE_AUTO_TIER_SYSTEM=true
